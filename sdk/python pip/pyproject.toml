[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xr2-sdk"
version = "0.2.6"
description = "Official Python SDK for xR2 — prompt management platform for AI applications"
readme = "README.md"
requires-python = ">=3.9"
authors = [{ name = "xR2", email = "hello@xr2.uk" }]
license = { text = "MIT" }
keywords = ["xr2", "prompts management", "sdk"]
dependencies = [
  "requests>=2.31.0",
  "httpx>=0.27.0",
  "pydantic>=2.5.0",
  "typing-extensions>=4.8.0",
]

[tool.setuptools]
packages = ["xr2_sdk"]
package-dir = {"" = "."}

